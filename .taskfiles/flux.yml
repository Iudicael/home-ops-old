---
version: "3"

tasks:
  sync:
    desc: Sync flux-system with the Git Repository
    cmds:
      - flux reconcile source git flux-system
    silent: true

  bootstrap:
    desc: bootstrap flux
    cmds:
      - kubectl --kubeconfig=./cluster/kubeconfig apply --kustomize=./cluster/base/flux-system
